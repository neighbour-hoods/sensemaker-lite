{
  "name": "sensemaker_happ-dev",
  "private": true,
  "workspaces": [
    "ui",
    "tests",
    "client"
  ],
  "scripts": {
    "start": "npm run network 2",
    "network": "hc s clean && npm run build:happ && concurrently-repeat \"npm run start:agent\"",
    "start:agent": "cross-env HC_PORT=$(port) concurrently -k \"npm run start:happ\" \"sleep 5 && cd ui && npm run start\"",
    "test": "npm run build:happ && npm run build:client && cd tests && npm t",
    "start:happ": "concurrently \"RUST_LOG=warn echo \"pass\" | hc s --piped generate ./workdir/sensemaker_happ.happ --run=$HC_PORT -a sensemaker_happ network mdns\" \"npm run playground\"",
    "package": "npm run build:happ && cd ui && npm run package && cd .. && hc web-app pack workdir",
    "build:happ": "npm run build:dnas && hc app pack ./workdir",
    "build:dnas": "npm run build:zomes && hc dna pack ./dnas/sensemaker/workdir && hc dna pack ./dnas/test_provider/workdir",
    "build:zomes": "CARGO_TARGET_DIR=target cargo build --release --target wasm32-unknown-unknown",
    "build:client": "cd client && npm run build",
    "playground": "run-singleton \"holochain-playground\""
  },
  "devDependencies": {
    "@holochain-playground/cli": "^0.0.11",
    "concurrently": "^6.2.1",
    "concurrently-repeat": "^0.0.1",
    "cross-env": "^7.0.3",
    "new-port-cli": "^1.0.0",
    "rimraf": "^3.0.2",
    "run-singleton-cli": "^0.0.5"
  },
  "engines": {
    "npm": ">=7.0.0"
  }
}
